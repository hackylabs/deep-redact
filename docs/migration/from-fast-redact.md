<!-- This file is generated by scripts/generate-fast-redact-migration-doc.ts. This file is generated. Do not edit it by hand. -->

# Migrating From fast-redact

This guide is generated from `test/migration/fast-redact/matrix.json` and the fixture directories under `test/migration/fast-redact/fixtures/`. The same manifest and fixtures drive verification, so documentation drift fails the generated-file checks.

Deep Redact v4 migration rows are grouped as direct equivalents, mechanical rewrites, and intentional divergences. Direct equivalents are parity cases, mechanical rewrites require a small configuration change, and intentional divergences need a deliberate caller decision before migration.

This guide covers documented `fast-redact` scenarios only. Deep Redact v3 migration, worked examples, benchmark evidence, and platform-adoption guidance are outside this guide.

## Summary

- Source package: `fast-redact@3.5.0`
- Canonical manifest: `test/migration/fast-redact/matrix.json`
- Fixture root: `test/migration/fast-redact/fixtures`
- Direct equivalents: 8
- Mechanical rewrites: 5
- Intentional divergences: 4

## Intentional Divergences

- `restore` is unsupported.
- `strict` is unsupported.
- omitted `serialise` returns structured output.
- Non-string literal `censor` values need an explicit migration action.

## Matrix

| Row | Classification | Assertion mode |
| --- | --- | --- |
| `dot-path-structured-output` | `direct-equivalent` | `same-structured-output` |
| `bracket-quoted-property-path` | `direct-equivalent` | `same-structured-output` |
| `numeric-array-index-path` | `direct-equivalent` | `same-structured-output` |
| `final-wildcard-path` | `direct-equivalent` | `same-structured-output` |
| `intermediate-wildcard-path` | `direct-equivalent` | `same-structured-output` |
| `leading-bracket-property-path` | `direct-equivalent` | `same-structured-output` |
| `literal-string-censor` | `direct-equivalent` | `same-structured-output` |
| `function-censor-helper` | `direct-equivalent` | `same-structured-output` |
| `bracket-array-wildcard-rewrite` | `mechanical-rewrite` | `same-structured-output` |
| `serialize-false-spelling-rewrite` | `mechanical-rewrite` | `same-structured-output` |
| `default-json-output-serialise-true` | `mechanical-rewrite` | `same-serialised-output` |
| `custom-serialize-function-rewrite` | `mechanical-rewrite` | `same-serialised-output` |
| `remove-true-rewrite` | `mechanical-rewrite` | `same-serialised-output` |
| `no-restore-api` | `intentional-divergence` | `documented-divergence` |
| `no-strict-option` | `intentional-divergence` | `v4-initialisation-error` |
| `omitted-serialise-structured-output` | `intentional-divergence` | `documented-divergence` |
| `non-string-censor-value` | `intentional-divergence` | `v4-initialisation-error` |

## Rows

### dot-path-structured-output

Classification: `direct-equivalent`

Assertion mode: `same-structured-output`

Fixture directory: `test/migration/fast-redact/fixtures/dot-path-structured-output`

fast-redact configuration:

```json
{
  "paths": [
    "user.password"
  ],
  "serialize": false
}
```

Deep Redact v4 action:

```json
{
  "kind": "deep-redact-config",
  "config": {
    "paths": [
      "user.password"
    ]
  },
  "migrationSteps": [
    "Keep the dot-path selector unchanged."
  ],
  "fastRedactBehaviour": "With serialize: false, fast-redact returns the redacted object for the configured dot path.",
  "v4Behaviour": "Deep Redact v4 returns the same structured redacted object for the same selector.",
  "reason": "The selector syntax and structured result are equivalent."
}
```

Expected result or divergence artefact:

```json
{
  "user": {
    "name": "Ada",
    "password": "[REDACTED]"
  },
  "requestId": "req-1"
}
```

Migration steps:

- Keep the dot-path selector unchanged.

fast-redact behaviour: With serialize: false, fast-redact returns the redacted object for the configured dot path.

Deep Redact v4 behaviour: Deep Redact v4 returns the same structured redacted object for the same selector.

Reason: The selector syntax and structured result are equivalent.

Notes: Dot-path targeting migrates unchanged when the old call already requested structured output with `serialize: false`.

### bracket-quoted-property-path

Classification: `direct-equivalent`

Assertion mode: `same-structured-output`

Fixture directory: `test/migration/fast-redact/fixtures/bracket-quoted-property-path`

fast-redact configuration:

```json
{
  "paths": [
    "headers[\"X-Forwarded-For\"]"
  ],
  "serialize": false
}
```

Deep Redact v4 action:

```json
{
  "kind": "deep-redact-config",
  "config": {
    "paths": [
      "headers[\"X-Forwarded-For\"]"
    ]
  },
  "migrationSteps": [
    "Keep the bracket-quoted property selector unchanged."
  ],
  "fastRedactBehaviour": "fast-redact targets the quoted property name exactly.",
  "v4Behaviour": "Deep Redact v4 targets the same quoted property name exactly.",
  "reason": "The quoted property path syntax is shared."
}
```

Expected result or divergence artefact:

```json
{
  "headers": {
    "X-Forwarded-For": "[REDACTED]",
    "User-Agent": "fixture-client"
  },
  "requestId": "req-2"
}
```

Migration steps:

- Keep the bracket-quoted property selector unchanged.

fast-redact behaviour: fast-redact targets the quoted property name exactly.

Deep Redact v4 behaviour: Deep Redact v4 targets the same quoted property name exactly.

Reason: The quoted property path syntax is shared.

Notes: Bracket-quoted property paths are retained exactly so headers and other punctuation-heavy keys stay readable.

### numeric-array-index-path

Classification: `direct-equivalent`

Assertion mode: `same-structured-output`

Fixture directory: `test/migration/fast-redact/fixtures/numeric-array-index-path`

fast-redact configuration:

```json
{
  "paths": [
    "accounts[0].token"
  ],
  "serialize": false
}
```

Deep Redact v4 action:

```json
{
  "kind": "deep-redact-config",
  "config": {
    "paths": [
      "accounts[0].token"
    ]
  },
  "migrationSteps": [
    "Keep the numeric array-index selector unchanged."
  ],
  "fastRedactBehaviour": "fast-redact redacts the selected array element only.",
  "v4Behaviour": "Deep Redact v4 redacts the same array element only.",
  "reason": "Numeric bracket indexes have the same migration meaning."
}
```

Expected result or divergence artefact:

```json
{
  "accounts": [
    {
      "name": "primary",
      "token": "[REDACTED]"
    },
    {
      "name": "backup",
      "token": "tok_backup"
    }
  ]
}
```

Migration steps:

- Keep the numeric array-index selector unchanged.

fast-redact behaviour: fast-redact redacts the selected array element only.

Deep Redact v4 behaviour: Deep Redact v4 redacts the same array element only.

Reason: Numeric bracket indexes have the same migration meaning.

Notes: Numeric array indexes migrate unchanged and continue to target only the named element.

### final-wildcard-path

Classification: `direct-equivalent`

Assertion mode: `same-structured-output`

Fixture directory: `test/migration/fast-redact/fixtures/final-wildcard-path`

fast-redact configuration:

```json
{
  "paths": [
    "tokens.*"
  ],
  "serialize": false
}
```

Deep Redact v4 action:

```json
{
  "kind": "deep-redact-config",
  "config": {
    "paths": [
      "tokens.*"
    ]
  },
  "migrationSteps": [
    "Keep the final-position wildcard selector unchanged."
  ],
  "fastRedactBehaviour": "fast-redact redacts every immediate child matched by the final wildcard.",
  "v4Behaviour": "Deep Redact v4 redacts the same immediate children.",
  "reason": "Final-position wildcard targeting is equivalent for this documented shape."
}
```

Expected result or divergence artefact:

```json
{
  "tokens": {
    "access": "[REDACTED]",
    "refresh": "[REDACTED]"
  },
  "status": "active"
}
```

Migration steps:

- Keep the final-position wildcard selector unchanged.

fast-redact behaviour: fast-redact redacts every immediate child matched by the final wildcard.

Deep Redact v4 behaviour: Deep Redact v4 redacts the same immediate children.

Reason: Final-position wildcard targeting is equivalent for this documented shape.

Notes: Final-position wildcards migrate unchanged for one-level child redaction.

### intermediate-wildcard-path

Classification: `direct-equivalent`

Assertion mode: `same-structured-output`

Fixture directory: `test/migration/fast-redact/fixtures/intermediate-wildcard-path`

fast-redact configuration:

```json
{
  "paths": [
    "users.*.password"
  ],
  "serialize": false
}
```

Deep Redact v4 action:

```json
{
  "kind": "deep-redact-config",
  "config": {
    "paths": [
      "users.*.password"
    ]
  },
  "migrationSteps": [
    "Keep the intermediate wildcard selector unchanged."
  ],
  "fastRedactBehaviour": "fast-redact redacts matching descendants beneath each wildcard segment.",
  "v4Behaviour": "Deep Redact v4 redacts the same descendant values.",
  "reason": "The shared wildcard string syntax is accepted by both libraries."
}
```

Expected result or divergence artefact:

```json
{
  "users": {
    "alice": {
      "name": "Alice",
      "password": "[REDACTED]"
    },
    "bob": {
      "name": "Bob",
      "password": "[REDACTED]"
    }
  },
  "requestId": "req-5"
}
```

Migration steps:

- Keep the intermediate wildcard selector unchanged.

fast-redact behaviour: fast-redact redacts matching descendants beneath each wildcard segment.

Deep Redact v4 behaviour: Deep Redact v4 redacts the same descendant values.

Reason: The shared wildcard string syntax is accepted by both libraries.

Notes: Intermediate wildcard paths migrate unchanged when both libraries accept the same string syntax.

### leading-bracket-property-path

Classification: `direct-equivalent`

Assertion mode: `same-structured-output`

Fixture directory: `test/migration/fast-redact/fixtures/leading-bracket-property-path`

fast-redact configuration:

```json
{
  "paths": [
    "[\"session\"].token"
  ],
  "serialize": false
}
```

Deep Redact v4 action:

```json
{
  "kind": "deep-redact-config",
  "config": {
    "paths": [
      "[\"session\"].token"
    ]
  },
  "migrationSteps": [
    "Keep the leading bracket-quoted property selector unchanged."
  ],
  "fastRedactBehaviour": "fast-redact accepts a leading bracket-quoted property selector and redacts the selected descendant.",
  "v4Behaviour": "Deep Redact v4 accepts the same leading bracket-quoted property selector and redacts the selected descendant.",
  "reason": "The leading bracket-quoted property path syntax is shared."
}
```

Expected result or divergence artefact:

```json
{
  "session": {
    "token": "[REDACTED]",
    "scope": "admin"
  },
  "requestId": "req-6"
}
```

Migration steps:

- Keep the leading bracket-quoted property selector unchanged.

fast-redact behaviour: fast-redact accepts a leading bracket-quoted property selector and redacts the selected descendant.

Deep Redact v4 behaviour: Deep Redact v4 accepts the same leading bracket-quoted property selector and redacts the selected descendant.

Reason: The leading bracket-quoted property path syntax is shared.

Notes: Leading bracket-quoted property paths migrate unchanged when the selector starts at a punctuation-heavy root key.

### literal-string-censor

Classification: `direct-equivalent`

Assertion mode: `same-structured-output`

Fixture directory: `test/migration/fast-redact/fixtures/literal-string-censor`

fast-redact configuration:

```json
{
  "paths": [
    "token"
  ],
  "censor": "[MASKED]",
  "serialize": false
}
```

Deep Redact v4 action:

```json
{
  "kind": "deep-redact-config",
  "config": {
    "paths": [
      "token"
    ],
    "censor": "[MASKED]"
  },
  "migrationSteps": [
    "Keep the literal string censor unchanged."
  ],
  "fastRedactBehaviour": "fast-redact replaces the selected value with the configured string.",
  "v4Behaviour": "Deep Redact v4 replaces the selected value with the same string.",
  "reason": "String censors are supported by both libraries."
}
```

Expected result or divergence artefact:

```json
{
  "token": "[MASKED]",
  "scope": "read"
}
```

Migration steps:

- Keep the literal string censor unchanged.

fast-redact behaviour: fast-redact replaces the selected value with the configured string.

Deep Redact v4 behaviour: Deep Redact v4 replaces the selected value with the same string.

Reason: String censors are supported by both libraries.

Notes: String censors migrate directly because both libraries replace the selected value with the configured string.

### function-censor-helper

Classification: `direct-equivalent`

Assertion mode: `same-structured-output`

Fixture directory: `test/migration/fast-redact/fixtures/function-censor-helper`

fast-redact configuration:

```json
{
  "paths": [
    "token"
  ],
  "censor": "[Function fixture helper: lastFourTokenCensor]",
  "serialize": false
}
```

Deep Redact v4 action:

```json
{
  "kind": "deep-redact-config",
  "config": {
    "paths": [
      "token"
    ],
    "censor": "[Function fixture helper: lastFourTokenCensor]"
  },
  "migrationSteps": [
    "Keep the function censor intent and map it to a declared Deep Redact v4 function."
  ],
  "fastRedactBehaviour": "fast-redact calls the configured censor function for the selected value.",
  "v4Behaviour": "Deep Redact v4 calls the equivalent configured censor function for the selected value.",
  "reason": "The fixture helper proves compatible one-argument censor behaviour without embedding source code in JSON."
}
```

Expected result or divergence artefact:

```json
{
  "token": "last:2345",
  "scope": "write"
}
```

Migration steps:

- Keep the function censor intent and map it to a declared Deep Redact v4 function.

fast-redact behaviour: fast-redact calls the configured censor function for the selected value.

Deep Redact v4 behaviour: Deep Redact v4 calls the equivalent configured censor function for the selected value.

Reason: The fixture helper proves compatible one-argument censor behaviour without embedding source code in JSON.

Notes: The function censor is represented by a declared helper ID so executable code does not live in the JSON manifest.

### bracket-array-wildcard-rewrite

Classification: `mechanical-rewrite`

Assertion mode: `same-structured-output`

Fixture directory: `test/migration/fast-redact/fixtures/bracket-array-wildcard-rewrite`

fast-redact configuration:

```json
{
  "paths": [
    "accounts[*].secret"
  ],
  "serialize": false
}
```

Deep Redact v4 action:

```json
{
  "kind": "deep-redact-config",
  "config": {
    "paths": [
      "accounts.*.secret"
    ]
  },
  "migrationSteps": [
    "Rewrite the bracket array wildcard path from `accounts[*].secret` to `accounts.*.secret`.",
    "Omit serialise for structured Deep Redact v4 output."
  ],
  "fastRedactBehaviour": "fast-redact accepts bracket array wildcard syntax.",
  "v4Behaviour": "Deep Redact v4 accepts the equivalent dot wildcard syntax.",
  "reason": "Deep Redact v4 rejects bracket wildcard syntax, so the selector needs a mechanical path rewrite."
}
```

Expected result or divergence artefact:

```json
{
  "accounts": [
    {
      "name": "primary",
      "secret": "[REDACTED]"
    },
    {
      "name": "backup",
      "secret": "[REDACTED]"
    }
  ],
  "requestId": "req-8"
}
```

Migration steps:

- Rewrite the bracket array wildcard path from `accounts[*].secret` to `accounts.*.secret`.
- Omit serialise for structured Deep Redact v4 output.

fast-redact behaviour: fast-redact accepts bracket array wildcard syntax.

Deep Redact v4 behaviour: Deep Redact v4 accepts the equivalent dot wildcard syntax.

Reason: Deep Redact v4 rejects bracket wildcard syntax, so the selector needs a mechanical path rewrite.

Notes: The documented `a[*].c.d` style becomes the Deep Redact v4 dot wildcard form, represented here as `accounts.*.secret`.

### serialize-false-spelling-rewrite

Classification: `mechanical-rewrite`

Assertion mode: `same-structured-output`

Fixture directory: `test/migration/fast-redact/fixtures/serialize-false-spelling-rewrite`

fast-redact configuration:

```json
{
  "paths": [
    "user.password"
  ],
  "serialize": false
}
```

Deep Redact v4 action:

```json
{
  "kind": "deep-redact-config",
  "config": {
    "paths": [
      "user.password"
    ],
    "serialise": false
  },
  "migrationSteps": [
    "Rename `serialize` to `serialise`.",
    "Keep the explicit false value when preserving structured output intent."
  ],
  "fastRedactBehaviour": "fast-redact uses the American-English `serialize` option.",
  "v4Behaviour": "Deep Redact v4 uses the British-English `serialise` option.",
  "reason": "The output intent is the same, but the public option spelling changes."
}
```

Expected result or divergence artefact:

```json
{
  "user": {
    "name": "Ada",
    "password": "[REDACTED]"
  },
  "requestId": "req-9"
}
```

Migration steps:

- Rename `serialize` to `serialise`.
- Keep the explicit false value when preserving structured output intent.

fast-redact behaviour: fast-redact uses the American-English `serialize` option.

Deep Redact v4 behaviour: Deep Redact v4 uses the British-English `serialise` option.

Reason: The output intent is the same, but the public option spelling changes.

Notes: The option spelling changes from `serialize` to `serialise`; the explicit false value keeps structured output intent visible.

### default-json-output-serialise-true

Classification: `mechanical-rewrite`

Assertion mode: `same-serialised-output`

Fixture directory: `test/migration/fast-redact/fixtures/default-json-output-serialise-true`

fast-redact configuration:

```json
{
  "paths": [
    "user.password"
  ]
}
```

Deep Redact v4 action:

```json
{
  "kind": "deep-redact-config",
  "config": {
    "paths": [
      "user.password"
    ],
    "serialise": true
  },
  "migrationSteps": [
    "Set `serialise: true` when the old `fast-redact` call relied on default JSON string output."
  ],
  "fastRedactBehaviour": "fast-redact serialises with JSON.stringify by default.",
  "v4Behaviour": "Deep Redact v4 returns structured output by default, so JSON string output must be requested explicitly.",
  "reason": "The default output type changed and needs an explicit option in migrated code."
}
```

Expected result or divergence artefact:

```text
{"user":{"name":"Ada","password":"[REDACTED]"},"requestId":"req-10"}
```

Migration steps:

- Set `serialise: true` when the old `fast-redact` call relied on default JSON string output.

fast-redact behaviour: fast-redact serialises with JSON.stringify by default.

Deep Redact v4 behaviour: Deep Redact v4 returns structured output by default, so JSON string output must be requested explicitly.

Reason: The default output type changed and needs an explicit option in migrated code.

Notes: Calls that relied on fast-redact's default JSON output need `serialise: true` in Deep Redact v4.

### custom-serialize-function-rewrite

Classification: `mechanical-rewrite`

Assertion mode: `same-serialised-output`

Fixture directory: `test/migration/fast-redact/fixtures/custom-serialize-function-rewrite`

fast-redact configuration:

```json
{
  "paths": [
    "user.password"
  ],
  "serialize": "[Function fixture helper: stableEnvelopeSerialiser]"
}
```

Deep Redact v4 action:

```json
{
  "kind": "deep-redact-config",
  "config": {
    "paths": [
      "user.password"
    ],
    "serialise": "[Function fixture helper: stableEnvelopeSerialiser]"
  },
  "migrationSteps": [
    "Rename `serialize` to `serialise`.",
    "Move the existing custom serialiser function to the Deep Redact v4 option."
  ],
  "fastRedactBehaviour": "fast-redact passes the redacted value to the configured serialize function.",
  "v4Behaviour": "Deep Redact v4 passes the redacted value to the configured serialise function.",
  "reason": "The serialiser function role is equivalent after the option is renamed."
}
```

Expected result or divergence artefact:

```text
stable:{"user":{"name":"Ada","password":"[REDACTED]"},"requestId":"req-11"}
```

Migration steps:

- Rename `serialize` to `serialise`.
- Move the existing custom serialiser function to the Deep Redact v4 option.

fast-redact behaviour: fast-redact passes the redacted value to the configured serialize function.

Deep Redact v4 behaviour: Deep Redact v4 passes the redacted value to the configured serialise function.

Reason: The serialiser function role is equivalent after the option is renamed.

Notes: The custom serialiser is represented by a declared helper ID and moves from `serialize` to `serialise`.

### remove-true-rewrite

Classification: `mechanical-rewrite`

Assertion mode: `same-serialised-output`

Fixture directory: `test/migration/fast-redact/fixtures/remove-true-rewrite`

fast-redact configuration:

```json
{
  "paths": [
    "user.password"
  ],
  "remove": true
}
```

Deep Redact v4 action:

```json
{
  "kind": "deep-redact-config",
  "config": {
    "paths": [
      "user.password"
    ],
    "remove": true,
    "serialise": true
  },
  "migrationSteps": [
    "Keep `remove: true`.",
    "Set `serialise: true` because fast-redact remove output is tied to JSON serialisation."
  ],
  "fastRedactBehaviour": "fast-redact removes the selected property from its default JSON string output.",
  "v4Behaviour": "Deep Redact v4 removes the selected property and serialises when `serialise: true` is set.",
  "reason": "Removal intent is equivalent, but migrated code must request serialisation explicitly."
}
```

Expected result or divergence artefact:

```text
{"user":{"name":"Ada"},"requestId":"req-12"}
```

Migration steps:

- Keep `remove: true`.
- Set `serialise: true` because fast-redact remove output is tied to JSON serialisation.

fast-redact behaviour: fast-redact removes the selected property from its default JSON string output.

Deep Redact v4 behaviour: Deep Redact v4 removes the selected property and serialises when `serialise: true` is set.

Reason: Removal intent is equivalent, but migrated code must request serialisation explicitly.

Notes: `remove: true` keeps the removal intent, while `serialise: true` preserves fast-redact's default JSON string output.

### no-restore-api

Classification: `intentional-divergence`

Assertion mode: `documented-divergence`

Fixture directory: `test/migration/fast-redact/fixtures/no-restore-api`

fast-redact configuration:

```json
{
  "paths": [
    "user.password"
  ],
  "serialize": false
}
```

Deep Redact v4 action:

```json
{
  "kind": "documented-divergence",
  "config": {
    "paths": [
      "user.password"
    ]
  },
  "migrationSteps": [
    "Do not migrate code that calls `restore` to an equivalent Deep Redact v4 API.",
    "Keep an unredacted source value separately if application logic still needs it."
  ],
  "fastRedactBehaviour": "With serialize: false, fast-redact exposes a redactor.restore method for caller-side reversal.",
  "v4Behaviour": "Deep Redact v4 redactors expose no restore, unredact, or reverse-operation method.",
  "reason": "Deep Redact v4 is intentionally one-way."
}
```

Expected result or divergence artefact:

```json
{
  "fastRedactBehaviour": "With serialize: false, fast-redact exposes a redactor.restore method for caller-side reversal.",
  "v4Behaviour": "Deep Redact v4 redactors expose no restore, unredact, or reverse-operation method.",
  "migrationAction": "Do not migrate code that calls `restore` to an equivalent Deep Redact v4 API. Keep an unredacted source value separately if application logic still needs it.",
  "reason": "Deep Redact v4 is intentionally one-way."
}
```

Migration steps:

- Do not migrate code that calls `restore` to an equivalent Deep Redact v4 API.
- Keep an unredacted source value separately if application logic still needs it.

fast-redact behaviour: With serialize: false, fast-redact exposes a redactor.restore method for caller-side reversal.

Deep Redact v4 behaviour: Deep Redact v4 redactors expose no restore, unredact, or reverse-operation method.

Reason: Deep Redact v4 is intentionally one-way.

Notes: This row verifies that the migration guide treats restore as an intentional divergence, not a compatibility target.

### no-strict-option

Classification: `intentional-divergence`

Assertion mode: `v4-initialisation-error`

Fixture directory: `test/migration/fast-redact/fixtures/no-strict-option`

fast-redact configuration:

```json
{
  "paths": [
    "missing.value"
  ],
  "strict": false,
  "serialize": false
}
```

Deep Redact v4 action:

```json
{
  "kind": "unsupported-v4-config",
  "config": {
    "paths": [
      "missing.value"
    ],
    "strict": false
  },
  "unsupportedOption": "strict",
  "migrationSteps": [
    "Remove `strict` from the Deep Redact v4 configuration.",
    "Handle primitive inputs according to the migrated caller's policy before calling Deep Redact v4."
  ],
  "fastRedactBehaviour": "With strict: false and serialize: false, fast-redact returns the primitive input unchanged when the redactor sees a primitive instead of an object.",
  "v4Behaviour": "Deep Redact v4 rejects the unsupported `strict` option during initialisation.",
  "reason": "Deep Redact v4 does not provide a strict compatibility switch for primitive input handling."
}
```

Expected result or divergence artefact:

```json
{
  "fastRedactBehaviour": "With strict: false and serialize: false, fast-redact returns the primitive input unchanged when the redactor sees a primitive instead of an object.",
  "v4Behaviour": "Deep Redact v4 rejects the unsupported `strict` option during initialisation.",
  "migrationAction": "Remove `strict` from the Deep Redact v4 configuration. Handle primitive inputs according to the migrated caller's policy before calling Deep Redact v4.",
  "reason": "Deep Redact v4 does not provide a strict compatibility switch for primitive input handling."
}
```

Migration steps:

- Remove `strict` from the Deep Redact v4 configuration.
- Handle primitive inputs according to the migrated caller's policy before calling Deep Redact v4.

fast-redact behaviour: With strict: false and serialize: false, fast-redact returns the primitive input unchanged when the redactor sees a primitive instead of an object.

Deep Redact v4 behaviour: Deep Redact v4 rejects the unsupported `strict` option during initialisation.

Reason: Deep Redact v4 does not provide a strict compatibility switch for primitive input handling.

Notes: This row keeps `strict` out of the v4 public API and documents the primitive-input behaviour that migrated callers must handle explicitly.

### omitted-serialise-structured-output

Classification: `intentional-divergence`

Assertion mode: `documented-divergence`

Fixture directory: `test/migration/fast-redact/fixtures/omitted-serialise-structured-output`

fast-redact configuration:

```json
{
  "paths": [
    "user.password"
  ]
}
```

Deep Redact v4 action:

```json
{
  "kind": "documented-divergence",
  "config": {
    "paths": [
      "user.password"
    ]
  },
  "migrationSteps": [
    "Set `serialise: true` in Deep Redact v4 if the old call depended on the fast-redact default JSON string.",
    "Leave `serialise` omitted only when the migrated caller wants structured output."
  ],
  "fastRedactBehaviour": "fast-redact returns a JSON string when `serialize` is omitted.",
  "v4Behaviour": "Deep Redact v4 returns structured output when `serialise` is omitted.",
  "reason": "The default output type intentionally differs."
}
```

Expected result or divergence artefact:

```json
{
  "fastRedactBehaviour": "fast-redact returns a JSON string when `serialize` is omitted.",
  "v4Behaviour": "Deep Redact v4 returns structured output when `serialise` is omitted.",
  "migrationAction": "Set `serialise: true` in Deep Redact v4 if the old call depended on the fast-redact default JSON string. Leave `serialise` omitted only when the migrated caller wants structured output.",
  "reason": "The default output type intentionally differs.",
  "expectedV4": {
    "user": {
      "name": "Ada",
      "password": "[REDACTED]"
    },
    "requestId": "req-15"
  }
}
```

Migration steps:

- Set `serialise: true` in Deep Redact v4 if the old call depended on the fast-redact default JSON string.
- Leave `serialise` omitted only when the migrated caller wants structured output.

fast-redact behaviour: fast-redact returns a JSON string when `serialize` is omitted.

Deep Redact v4 behaviour: Deep Redact v4 returns structured output when `serialise` is omitted.

Reason: The default output type intentionally differs.

Notes: This row distinguishes the intentional default-output divergence from the mechanical rewrite that adds `serialise: true`.

### non-string-censor-value

Classification: `intentional-divergence`

Assertion mode: `v4-initialisation-error`

Fixture directory: `test/migration/fast-redact/fixtures/non-string-censor-value`

fast-redact configuration:

```json
{
  "paths": [
    "user.password"
  ],
  "censor": 0,
  "serialize": false
}
```

Deep Redact v4 action:

```json
{
  "kind": "unsupported-v4-config",
  "config": {
    "paths": [
      "user.password"
    ],
    "censor": 0
  },
  "unsupportedOption": "censor",
  "migrationSteps": [
    "Replace non-string literal censors with a string censor or a function censor before migrating.",
    "Use a function censor if the migrated code needs to return a non-string value deliberately."
  ],
  "fastRedactBehaviour": "fast-redact accepts documented non-string censor values such as number literals.",
  "v4Behaviour": "Deep Redact v4 rejects root censor values that are neither strings nor functions.",
  "reason": "The v4 public Censor contract is intentionally narrower."
}
```

Expected result or divergence artefact:

```json
{
  "fastRedactBehaviour": "fast-redact accepts documented non-string censor values such as number literals.",
  "v4Behaviour": "Deep Redact v4 rejects root censor values that are neither strings nor functions.",
  "migrationAction": "Replace non-string literal censors with a string censor or a function censor before migrating. Use a function censor if the migrated code needs to return a non-string value deliberately.",
  "reason": "The v4 public Censor contract is intentionally narrower."
}
```

Migration steps:

- Replace non-string literal censors with a string censor or a function censor before migrating.
- Use a function censor if the migrated code needs to return a non-string value deliberately.

fast-redact behaviour: fast-redact accepts documented non-string censor values such as number literals.

Deep Redact v4 behaviour: Deep Redact v4 rejects root censor values that are neither strings nor functions.

Reason: The v4 public Censor contract is intentionally narrower.

Notes: This row documents the current v4 limitation for non-string fast-redact censor values without widening the runtime contract.


